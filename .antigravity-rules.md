# HRMS Assessment Rules

STRICT RULES

1. NEVER modify boilerplate architecture.

2. NEVER rename existing folders.

3. NEVER delete existing code.

4. ALWAYS copy TodoFeature structure when creating a new module.

5. ALWAYS use existing GraphQL setup.

6. ALWAYS use existing MediatR pattern.

7. ALWAYS use existing PostgreSQL repository pattern.

8. ALWAYS use existing dependency injection pattern.

9. ONLY create new files when necessary.

10. Keep implementations simple and interview-explainable.

11. Reuse DataTable for all list pages.

12. Reuse existing auth middleware.

13. Reuse existing Apollo client.

14. Reuse existing Redux store.

15. Do not introduce new UI libraries.

16. Use Tailwind only.

17. Generate small changes module by module.

18. Stop after every completed task.

# HRMS Assessment Rules

STRICT RULES

1. NEVER modify or replace the provided boilerplate architecture.

2. NEVER rename existing folders.

3. NEVER delete existing files.

4. ALWAYS clone TodoFeature architecture when creating backend modules.

5. ALWAYS reuse GraphQL, MediatR, PostgreSQL repository patterns.

6. ALWAYS reuse existing Apollo client.

7. ALWAYS reuse existing Redux setup.

8. ALWAYS reuse authentication middleware.

9. ALWAYS reuse existing DataTable component.

10. ALWAYS use Tailwind CSS.

11. NEVER install new UI libraries.

12. NEVER create a new project.

13. NEVER replace Next.js configuration.

14. NEVER modify Middleware, Extensions, Telemetry, Shared folders unless explicitly required.

15. NEVER modify Program.cs or Startup.cs without asking first.

16. Keep code interview-explainable.

17. Build module by module.

18. Prefer simple implementations over perfect implementations.

19. Stop after each completed checkpoint.